# Net Control by K4HEF - Environment Configuration Template
# Copy this file to .env and update the values

# JWT Secret Key
# IMPORTANT: Change this to a long, random string in production
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your_very_secure_jwt_secret_key_here_at_least_32_characters_long

# MongoDB Connection
# For Docker deployment (default):
MONGODB_URI=mongodb://mongodb:27017/netcontrol

# For local development (uncomment and use this instead):
# MONGODB_URI=mongodb://localhost:27017/netcontrol

# Server Port (default: 5000)
PORT=5000

# Node Environment
NODE_ENV=production

# Notes:
# - Never commit the .env file to version control
# - Keep your JWT_SECRET secure and unique
# - For production, use a strong JWT_SECRET (32+ characters)
# - QRZ API key is configured through the application Settings page

